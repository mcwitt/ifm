srcdir = @srcdir@
VPATH = $(srcdir)

INC = -I. -I$(srcdir)
CFLAGS = $(INC) -std=c99
LDFLAGS = -lgsl -lgslcblas -lm

# optimization
CFLAGS += -O3

# for debugging
#CFLAGS += -fsanitize=address

wolff: lattice.o neighbors.o state.o update.o meas_basic.o

clean:
	$(RM) *.o
